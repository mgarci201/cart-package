{% extends '::base.html.twig' %}

{% block body %}

{{ form_errors(form) }}
<div class="container">
    <h1>Package</h1>
    <table class="record_properties">
        <tbody>     
            <tr>
                <th>Id</th>
                <td>{{ form(form) }}</td>
            </tr>
            <tr>
                <th>Package</th>
                <td>{{ form(form) }}</td>
            </tr>
            <tr>
                <th>Numbers</th>
                <td>{{ form(form) }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ form(form) }}</td>
            </tr>
        </tbody>
    </table>
<ul class="record_actions">
    <li>
        <a href="{{ path('package') }}">Back to the list</a>
    </li>

</ul>
</div>
{% endblock %}
